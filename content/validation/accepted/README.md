# Accepted external and historical evidence

This directory is intentionally empty of accepted JSON records. A proposed or draft record belongs in an issue or working branch, not here.

`npm run validate:evidence` derives every public evidence count from accepted JSON records in this directory. It rejects records without a stable public report, an exact report hash, common claim boundaries, and category-specific proof fields.

