# Connector contract fixtures

Every file in this directory is a tiny, fictional structure fixture created for software tests. The values are not observations from the named publisher and must never be used as empirical evidence. Restricted API payloads are deliberately not committed.
